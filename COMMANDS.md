# Команды ↔ скиллы

Соответствие команд `commands.json` скиллам `cli/.opencode/skill/`.
Обозначения вызова: `[ключ]` — триггер (`алиса`, `пожалуйста`),
`_команда_` — слово после ключа (совпадение ≥90% с match-шаблоном),
`{настройка}` — слова после команды.

## Таблица соответствия

| Команда (commands.json) | Ключ (match) | Скилл (cli) | Ключ (скилл) | Что делает |
|---|---|---|---|---|
| `volumeup` | громче, сделай громче, увеличь громкость, громкость вверх | `media-volume-up` | громче, сделай громче, увеличь громкость, громкость вверх | увеличить громкость |
| `volumedown` | тише, сделай тише, уменьши громкость, громкость вниз | `media-volume-down` | тише, сделай тише, уменьши громкость, громкость вниз | уменьшить громкость |
| `playpause` | пауза, плей, играй, включи, дальше | `media-play-pause` | пауза, плей, играй, включи, дальше | play/pause медиа |
| `stop` | стоп, остановить, выключи | `media-stop` | стоп, остановить, выключи | остановить медиа |
| `openyoutube` | открой ютуб, запусти ютуб, включи ютуб | `youtube-open` | открой ютуб, запусти ютуб, включи ютуб | открыть YouTube |
| `youtube_news` | новости ютуб, новости на ютубе | `youtube-news` | новости ютуб, новости на ютубе, включи/покажи новости | искать и играть новости на YouTube |
| `news` (sequence) | включи новости, покажи новости, запусти новости | `youtube-news` (покрывает и sequence) | включи новости, покажи новости | YouTube + новости |
| `calendar-reminder` | напомни, напомни мне, напомнишь, напомним, поставь/создай/добавь напоминание | `calendar-reminder` | напомни ..., поставь напоминание, создай напоминание | напоминание = событие Google Calendar |
| `task-add` | создай/добавь задачу, создать/добавить задачу, запиши задачу, запиши/добавь в задачи, новая задача | `task-add` | добавь/создай/запиши задачу, новая задача | задача в Google Tasks (`@default`) |
| `diagnose` | анализ, диагностика, проверь логи, проверь ошибки, найди ошибки/ошибку | — (raw-агент opencode, без скилла) | — | анализ последнего лога + чата, автофикс; супервизор: WORKING.MD, вотчдог 600с, откат git, до 5 попыток; detached, голосовой цикл не блокируется |
| — нет команды | — | `calendar-plans` | что у меня сейчас по планам, по расписанию | текущее событие из календаря |
| — нет команды | — | `task-complete` | заверши/выполни/закрой задачу | отметить задачу выполненной |
| — нет команды | — | `browser-automation` | — | универсальный браузер (инструмент) |
| — нет команды | — | `speak-answer` | — | озвучка результата (сервисный) |

## Свободный текст (`{{text}}`)

Команды `calendar-reminder` и `task-add` принимают свободный текст —
все слова после команды подставляются в `{{text}}` shell-шаблона:

```
[Алиса] _напомни_ {через тридцать минут} {сходить в магазин}
```

- опция 1 — время/период текстом (`через тридцать минут`);
  если время не детектировано — напоминание на **+60 минут**;
- опция 2 — текст мероприятия (`сходить в магазин`).
- shell: `python -m lib.reminders "{{text}}"` →
  `ReminderHandler` + `GoogleCalendar.create_reminder`.

```
[пожалуйста] _создай задачу_ {Убраться у кошки}
```

- одна опция — текст задачи (`Убраться у кошки`).
- shell: `python -m lib.tasks create "{{text}}"` →
  `TaskHandler` + `GoogleTasks.create_task` (список `@default`).

```
[Алиса] _анализ_ / _диагностика_ / _проверь логи_
```

- без опций (или фокус текстом после команды).
- shell: `python -m lib.diagnose launch "{{text}}"` — мгновенный возврат:
  detached-супервизор (`lib/diagnose.py run`) в фоне.
- супервизор: путь последнего `logs/commands_*.log` + хвост +
  последние сообщения чата → задача агенту opencode (raw, корень репо):
  «проанализируй, найди и исправь ошибки», протокол WORKING.MD
  (создать → отмечать статус → удалить).
- правила: грязный `git status` — только озвучка
  «Сначала зафиксируй изменения в гит», запуска нет;
  WORKING.MD не правится >600с — прибить агента,
  `git checkout -- .`, заново (до 5 попыток);
  WORKING.MD пропал — прогон `pytest tests/ -q`: зелёные — финиш,
  красные — откат + задача назад агенту (хвост ошибок в промпте).

Настройки громкости: `{немного}` — шаг ×0.5, `{сильно}` — шаг ×2.
