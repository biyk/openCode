# Команды ↔ скиллы

Соответствие команд `commands.json` скиллам `cli/.opencode/skill/`.
Обозначения вызова: `[ключ]` — триггер (`алиса`, `пожалуйста`),
`_команда_` — слово после ключа (совпадение ≥90% с match-шаблоном),
`{настройка}` — слова после команды.

## Таблица соответствия

| Код | Ключи | Описание |
|---|---|---|
| `volumeup` | громче, сделай громче, увеличь громкость, громкость вверх | увеличить громкость |
| `volumedown` | тише, сделай тише, уменьши громкость, громкость вниз | уменьшить громкость |
| `sleepvolume` | — | ночной режим: опустить громкость до 1% |
| `monitoroff` | — | погасить экран (отключить монитор; разбудит любой ввод) |
| `sleepfix` | — | ночной режим: перенести начало события «СОН» на сейчас |
| `wakefix` | я проснулся, проснулся, я встал, доброе утро, время встать | пробуждение: перенести завершение начавшегося события «СОН» на сейчас и засчитать по ✅ задачу «Пробуждение» |
| `playpause` | пауза, плей, играй, включи, дальше | play/pause медиа |
| `stop` | стоп, остановить, выключи | остановить медиа |
| `openyoutube` | открой ютуб, запусти ютуб, включи ютуб | открыть YouTube |
| `youtube_news` | новости ютуб, новости на ютубе | искать и играть новости на YouTube |
| `calendar-reminder` | напомни, напомни мне, напомнишь, напомним, поставь напоминание, создай напоминание, добавь напоминание | напоминание = событие Google Calendar |
| `task-add` | поставь задачу, создай задачу, создай задача, добавь задачу, добавь задача, создать задачу, добавить задачу, запиши задачу, запиши в задачи, добавь в задачи, новая задача | задача в Google Tasks (@default) |
| `calendar-plans` | — | показать текущее или ближайшее событие календаря |
| `task-complete` | — | отметить задачу выполненной |
| `diagnose` | анализ, диагностика, проверь логи, проверь ошибки, найди ошибки, найди ошибку | анализ последнего лога и чата, автофикс; супервизор WORKING.MD, watchdog 600 секунд, откат git, до 5 попыток; detached, голосовой цикл не блокируется |
| `taskstart` | я начал, я приступил | запуск задачи: по названию найти сегодняшнее мероприятие, взять uuid из описания и записать start_date в Google Таблицу |
| `taskdone` | завершил, я завершил, завершил задачу, задача завершена, закончил задачу, я закончил задачу, сделал задачу, завершил дело | завершение задачи: без названия — текущая запущенная (⏹ по факту), с названием — сегодняшнее мероприятие; ⏹ если запущена, иначе ✅ по плану |
| `shop-add` | нужно купить, надо купить, купить, добавь в покупки, запиши в покупки | добавить товар в список покупок (Google Таблица «Список покупок») |
| `shop-bought` | купил, я купил, купили, мы купили | купил товар: удалить позицию из списка покупок |
| `news` | включи новости, покажи новости, запусти новости | Открыть ютуб и включить новости |
| `sleepmode` | спать, я спать, иду спать, я лег спать, лег спать, я пошел спать, пошел спать, я ложусь спать, ложусь спать | ночной режим: громкость до 1% + погасить экран + зафиксировать начало события «СОН» в календаре |
| `browser-automation` | — | универсальный браузер (инструмент) |
| `speak-answer` | — | озвучка результата (сервисный) |

## Свободный текст (`{{text}}`)

Команды `calendar-reminder` и `task-add` принимают свободный текст —
все слова после команды подставляются в `{{text}}` shell-шаблона:

```
[Алиса] _напомни_ {через тридцать минут} {сходить в магазин}
```

- опция 1 — время/период текстом (`через тридцать минут`);
  если время не детектировано — напоминание на **+60 минут**;
- опция 2 — текст мероприятия (`сходить в магазин`).
- shell: `python -m lib.reminders "{{text}}"` →
  `ReminderHandler` + `GoogleCalendar.create_reminder`.

```
[пожалуйста] _создай задачу_ {Убраться у кошки}
```

- одна опция — текст задачи (`Убраться у кошки`).
- shell: `python -m lib.tasks create "{{text}}"` →
  `TaskHandler` + `GoogleTasks.create_task` (список `@default`).

```
[Алиса] _анализ_ / _диагностика_ / _проверь логи_
```

- без опций (или фокус текстом после команды).
- shell: `python -m lib.diagnose launch "{{text}}"` — мгновенный возврат:
  detached-супервизор (`lib/diagnose.py run`) в фоне.
- супервизор: путь последнего `logs/commands_*.log` + хвост +
  последние сообщения чата → задача агенту opencode (raw, корень репо):
  «проанализируй, найди и исправь ошибки», протокол WORKING.MD
  (создать → отмечать статус → удалить).
- правила: грязный `git status` — только озвучка
  «Сначала зафиксируй изменения в гит», запуска нет;
  WORKING.MD не правится >600с — прибить агента,
  `git checkout -- .`, заново (до 5 попыток);
  WORKING.MD пропал — прогон `pytest tests/ -q`: зелёные — финиш,
  красные — откат + задача назад агенту (хвост ошибок в промпте).

Настройки громкости: `{немного}` — шаг ×0.5, `{сильно}` — шаг ×2.
