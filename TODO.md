# TODO

## Планируемые задачи

* \[ ] Создать/скопировать файл настроек opencode

---

## Не связано с TDD (из прежнего прогресса)
- AGENTS.md: упоминания про `LLM_TRIGGER`/`main.py:31` устарели (актуально: `triggers` из commands.json). README уже обновлён (коммит не сделан — проверь `git status`).
- `.idea/` и `targets/FLTP-5i3-16512/commands/` — untracked, не коммитить без запроса.
- Коммиты сделаны: `e942aef` (триггеры), `d7b0984` (вернул печать слов). README-правки пока НЕ закоммичены.