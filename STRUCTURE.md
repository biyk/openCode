# Project Structure

## STRUCTURE:START
```
voice
├── .opencode/  # Локальный конфиг OpenCode: навыки агента и плагины
│   └── skill/  # Навыки агента OpenCode: голосовой короткий ответ через TTS
│       └── speak-answer/  # Навык: голосовой короткий ответ пользователю через TTS в конце задачи
│           └── SKILL.md  # Описание навыка speak-answer: озвучивание короткого ответа пользователю через TTS — сообщает о выполнении или неудаче задачи.
├── cli/  # Консольный интерфейс проекта Voice Control, включая навыки и обработку команд
│   ├── .opencode/  # Конфиг OpenCode CLI: навыки fallback голосовых команд
│   │   └── skill/  # Навыки CLI‑фоллбэка: медиа, браузер, календарь, задачи, TTS‑ответ
│   │       ├── browser-automation/  # Навык автоматизации браузера через OpenCode Browser
│   │       │   └── SKILL.md  # Это файл описания навыка browser-automation для OpenCode, содержащий метаданные, инструкцию по использованию и примеры команд для автоматизации браузера.
│   │       ├── calendar-plans/  # Отчет о текущем или следующем задании из Google Calendar
│   │       │   └── SKILL.md  # Это файл описания навыка calendar‑plans в формате SKILL.md, который задаёт его название, описание, лицензии, совместимость и инструкции, позволяющие голосовому хосту получать и озвучивать текущие или ближайшие события из Google Calendar.
│   │       ├── calendar-reminder/  # Создает напоминание в Google Calendar по голосовой
│   │       │   └── SKILL.md  # Это файл SKILL.md, описывающий навык calendar‑reminder, который парсит голосовые запросы о напоминаниях и создает соответствующие события в Google Calendar.
│   │       ├── openyoutube/  # Навык для открытия YouTube в браузере через CDP. Если нет VPN или браузер, со...
│   │       │   └── openyoutube.md  # Это файл описания навыка openyoutube, содержащий метаданные и инструкцию по открытию главной страницы YouTube через CDP‑браузер в рамках голосового управления.
│   │       ├── playpause/  # Управляет состоянием воспроизведения/паузы медиа через CLI-команды
│   │       │   └── SKILL.md  # Это файл описания навыка playpause, содержащий метаданные и инструкцию по переключению воспроизведения/паузы медиа на Windows‑hostе через клавишу VK_MEDIA_PLAY_PAUSE.
│   │       ├── speak-answer/
│   │       │   └── SKILL.md  # Навык для озвучивания короткого человеческого ответа пользователю через TTS — сообщает о выполнении или неудаче задачи.
│   │       ├── stop/  # Останавливает или приостанавливает воспроизведение медиа на Windows-хосте
│   │       │   └── SKILL.md  # Файл описывает функциональность команды 'stop', которая останавливает или приостанавливает воспроизведение медиа.
│   │       ├── task-add/
│   │       │   └── SKILL.md  # Это файл описания навыка task‑add, содержащий метаданные и инструкцию по парсингу голосовых запросов и созданию задачи в Google Tasks через голосовой интерфейс.
│   │       ├── task-complete/
│   │       │   └── SKILL.md  # Это файл описания навыка task‑complete, содержащий метаданные, описание поведения и инструкцию по завершению задачи Google Tasks через голосовой интерфейс.
│   │       ├── volumedown/
│   │       │   └── SKILL.md  # Это файл описания навыка `volumedown`, содержащий метаданные и инструкцию по понижению системного громкости Windows через клавиши VK_VOLUME_DOWN, который используется в голосовом управлении для выполнения команды «тише».
│   │       ├── volumeup/
│   │       │   └── SKILL.md  # Это файл описания навыка volumeup, содержащий метаданные и инструкцию по повышению системной громкости Windows через скрипт.
│   │       └── youtube_news/
│   │           └── SKILL.md  # Это файл описания навыка youtube_news, содержащий метаданные и инструкции для голосового управления, позволяющий искать и воспроизводить новостные видео на YouTube.
│   └── AGENTS.md  # Файл описывает конфигурацию консольного интерфейса проекта Voice Control, включая роли, пути, пайплайн обработки команд и правила работы навыков (skills).
├── lib/
│   ├── providers/
│   │   ├── __init__.py  # Это модуль инициализации пакета провайдеров LLM, в котором объявлен базовый абстрактный класс клиента и экспортированы конкретные реализации клиентов.
│   │   ├── lmstudio.py  # Файл lib/providers/lmstudio.py содержит класс LmStudioClient, реализующий клиент для локального сервера LM Studio с OpenAI‑совместимым API, позволяя отправлять текстовые запросы, получать ответы модели и вести журнал истории.
│   │   ├── manager.py  # Менеджер LLM провайдеров с поддержкой переключения.
│   │   ├── omni.py  # Это файл, реализующий клиент OmniRouterClient для общения с локальным прокси‑сервером OmniRouter (OpenAI‑совместимым API) и интегрирующий его в систему логирования и вывода.
│   │   └── race.py  # Это файл, определяющий класс RaceClient, который параллельно отправляет запросы двум провайдерам LLM (OmniRouterClient и LmStudioClient) и возвращает первый полученный ответ, реализуя конкурентный запрос и логирование.
│   ├── __init__.py  # Это файл инициализации пакета lib, который задаёт атрибут __version__ для указания текущей версии проекта.
│   ├── aliases.py  # Это модуль, реализующий хранилище алиасов команд с поддержкой загрузки из файла, автообновления, подтверждения и учёта статистики.
│   ├── browser_control.py  # Это модуль, реализующий управление браузером через протокол CDP, позволяющий из командной строки открывать вкладки, выполнять JavaScript, кликать элементы и автоматизировать действия, например на YouTube.
│   ├── commands.py  # Содержит класс CommandMatcher — сопоставление голосовых команд с shell-командами. Загружает команды из JSON-файла, ищет совпадения по концепции трёх строк (ключ/команда/настройки) с порогом схожести 90%, поддерживает платформенно-специфичные шаблоны, составные команды (sequences), статусы requires/provides, настройки ({немноx}/{сильно}), а также конфигурацию LLM, intent, скиллов, opencode-cli и Google (calendar/tasks). Выполняет команды через subprocess с подтверждением и TTS.
│   ├── config_loader.py  # Это модуль‑загрузчик конфигурации, который определяет путь к файлу commands.json для указанного устройства, при необходимости создаёт каталог устройства и копирует туда файловый файл команд.
│   ├── diagnose.py  # Это модуль, реализующий команду диагностики логов, запускающий фонового супервизора, который анализирует логи, исправляет ошибки и управляет процессом через lock‑файл.
│   ├── google_calendar.py  # Это модуль, реализующий обёртку над Google Calendar API с OAuth‑авторизацией, позволяющий создавать напоминания и получать список событий, а также предоставляющий командную строку для вывода календаря.
│   ├── google_tasks.py  # Это файл‑модуль, реализующий обёртку над Google Tasks API с OAuth‑авторизацией и предоставляющий методы для создания, получения и завершения задач.
│   ├── intent.py  # Мини-слой классификации голосовой команды через LLM: IntentClassifier сопоставляет распознанный текст с id команды из списка (с учётом триггеров, статусов и ошибок STT) и возвращает id либо None (NONE → обычный диалог).
│   ├── logger.py  # Это модуль логирования, который сохраняет команды, сообщения LLM и их историю, а также предоставляет функции для получения последних логов и чтения истории чата.
│   ├── media.py  # Это модуль, который через PowerShell‑скрипт проверяет наличие и состояние медиа‑сессий в Windows, предоставляя функции для определения, воспроизводится ли медиа и есть ли активные медиа‑сессии, что необходимо для управления воспроизведением в проекте.
│   ├── opencode_cli.py  # Это модуль, реализующий класс для запуска и управления консольным CLI opencode, позволяющий выполнять пользовательские команды через модель и обрабатывать их вывод.
│   ├── orchestrator.py  # Это файл, реализующий класс Orchestrator, который управляет обработкой распознанного голосового ввода, определяя уровень команд (commands.json, алиасы, мини‑LLM, opencode), контролируя TTS, режим разработки и очередь фоновых запросов.
│   ├── output.py  # Это модуль, реализующий класс для вывода сообщений в консоль и записи их в лог‑файл, используемый в проекте для логирования работы ассистента.
│   ├── plans.py  # Это модуль lib/plans.py, реализующий обработчик голосового запроса о текущих планах, который получает актуальное событие из Google Calendar и возвращает его для озвучки.
│   ├── reminders.py  # Это модуль, реализующий обработку голосовых команд напоминаний, парсит время и текст, создаёт событие в Google Calendar и предоставляет CLI‑интерфейс.
│   ├── skills.py  # Это модуль, реализующий реестр и исполнителя статических скиллов, который загружает JSON‑манифесты, индексирует фразы, проверяет параметры и последовательно выполняет описанные действия.
│   ├── status.py  # Это файл lib/status.py, реализующий потокобезопасное хранилище статусов с фоновой проверкой и обновлением, позволяющее проекту отслеживать состояния (VPN, медиа, браузер и т.п.) и использовать их в командах.
│   ├── tasks.py  # Это модуль lib/tasks.py, реализующий обработку голосовых команд для создания и завершения задач в Google Tasks, включая парсинг, авторизацию и командный интерфейс.
│   ├── time_parser.py  # Парсинг времени и текста напоминания из русской голосовой команды.
│   ├── tts.py  # Это модуль, реализующий синтез речи с онлайн‑ и офлайн‑fallback, а также предоставляющий CLI‑интерфейс для озвучки текста.
│   ├── version.py  # Это модуль, который импортирует переменную __version__ из пакета lib и предоставляет функцию get_version() для получения текущей версии проекта.
│   ├── version_checker.py  # Это модуль lib/version_checker.py, который в отдельном потоке периодически проверяет совпадение версии приложения и проекта и завершает программу при обнаружении несоответствия.
│   └── version_gate.py  # Файл lib/version_gate.py отвечает за проверку соответствия версии приложения и версии проекта, считывает номер версии из файла VERSION и при несовпадении завершает работу.
├── prompts/
│   └── chat_template.txt  # Это файл шаблона подсказки для чат‑бота, определяющий стиль и правила ответов.
├── scripts/
│   ├── __init__.py  # Это инициализирующий файл пакета скриптов репозитория, который позволяет импортировать и использовать скрипты для генерации документации и повышения версий.
│   ├── bump_version.py  # Это скрипт, который повышает номер версии проекта (в файле VERSION и переменной __version__ в lib/__init__.py) согласно указанному типу (patch, minor, major) и выводит результат.
│   ├── check_tooltips.py  # Проверяет наличие описания у файлов и папок проекта в .structure.json
│   ├── generate_commands_md.py  # Это скрипт, который генерирует таблицу соответствия команд и навыков в файле COMMANDS.md и позволяет проверять её актуальность, выводить в stdout или обновлять файл.
│   ├── generate_structure.py  # Это скрипт, генерирующий и проверяющий файл STRUCTURE.md на основе манифеста .structure.json, выводя дерево проекта с русскими описаниями.
│   ├── get_tooltip.py  # и забираем всё после него до конца строки.
│   └── list_files.py  # <описание>' (или аналогичный однострочный/многострочный комментарий, "
├── targets/  # Конфиги голосовых команд для целевых хостов и устройств
│   ├── FLTP-5i3-16512/
│   │   ├── commands/  # Голосовые команды для Windows: громкость, пауза, стоп медиа, YouTube, календарь, задачи, диагностика
│   │   │   ├── mediastop.ps1  # Это PowerShell‑скрипт, который отправляет системный клавишный код Stop для медиа‑плеера и при необходимости через две секунды нажимает Play/Pause, чтобы гарантировать паузу.
│   │   │   ├── volumedown.ps1  # Это PowerShell‑скрипт, имитирующий нажатие клавиши уменьшения громкости в Windows, чтобы уменьшать системный звук на заданный процент.
│   │   │   └── volumeup.ps1  # Это PowerShell‑скрипт, повышающий громкость, имитируя нажатие клавиши Volume Up заданное количество раз.
│   │   ├── aliases.json  # Файл aliases.json содержит набор пользовательских алиасов команд и ожидающих подтверждения запросов, используемых в проекте для сопоставления голосовых фраз с внутренними командами.
│   │   ├── commands.json  # Голосовые команды для Windows: воспроизведение, громкость, YouTube, календарь, задачи, диагностика
│   │   └── status.json  # ОпределениеStatuses для целеага FLTP-5i3-16512 — VPN, медиа, медиа-сессия, браузер YouTube; каждый из них имеет checker, интервал проверки и сообщение о необходимости вмешательства.
│   ├── biyk-NB119/
│   │   └── commands.json  # Голосовые команды для хоста biyk-NB119: громкость, пауза, стоп
│   ├── my_pc/
│   │   └── commands.json  # Голосовые команды для моего ПК: управление медиа и громкостью
│   ├── test_device/  # Тестовый хост: управление громкостью и воспроизведением через PulseAudio
│   │   └── commands.json  # JSON‑файл, определяющий команды управления звуком и воспроизведением, их соответствия голосовым фразам и параметры LLM для тестового устройства.
│   └── commands.json  # Конфиги голосовых команд Google Calendar и Google Tasks, триггеры, команды, настройки приложений, маппинг, настройки LLM
├── tests/
│   ├── skills/  # Автотесты системы скиллов: загрузка, поиск, выполнение и проверки действий
│   │   ├── test_actions.py  # Это файл тестов, проверяющий действия скиллов open_url и run_cmd, их работу на разных ОС и обработку ошибок в классе SkillRegistry.
│   │   ├── test_execution.py  # Это файл тестов, проверяющий работу класса SkillRegistry, включая выполнение скиллов, валидацию параметров и обработку различных действий.
│   │   ├── test_loading.py  # Это файл тестов, проверяющий загрузку, индексацию и поиск скиллов в классе SkillRegistry.
│   │   ├── test_manifest.py  # Это тестовый модуль, проверяющий корректность создания и поведения dataclass SkillManifest и его компонентов.
│   │   └── test_skills_dir.py  # Тесты для функции get_skills_dir из модуля lib.skills. Проверяют, что функция возвращает корректный путь к папке skills с именем хоста.
│   ├── __init__.py  # Пакет tests – автотесты проекта
│   ├── test_aliases.py  # Это файл тестов, проверяющий работу класса AliasStore и функции normalize_core, обеспечивая корректную загрузку, разрешение, добавление, подтверждение, удаление и подсчёт использований алиасов.
│   ├── test_browser_control.py  # Это файл тестов для модуля lib/browser_control.py, проверяющий функции управления браузером через CDP и командную строку.
│   ├── test_check_tooltips.py  # Тесты для проверки наличия TOOLTIP-описаний в пакетах и модулях
│   ├── test_commands.py  # Это файл тестов, содержащий набор проверок для класса CommandMatcher из модуля lib.commands, который проверяет загрузку конфигураций, поиск и выполнение голосовых команд, обработку триггеров, параметров, статусов и интеграцию с LLM‑моделями.
│   ├── test_config_loader.py  # Это файл тестов, проверяющий функцию get_device_commands_path из модуля config_loader, обеспечивая корректную работу с путями файлов команд устройств.
│   ├── test_dependencies.py  # Это файл тестов, проверяющий, что библиотека sounddevice установлена и корректно работает.
│   ├── test_device_commands.py  # Это файл тестов, который проверяет корректность и соответствие команд, описанных в JSON‑файле device‑commands, включая их наличие, формат JSON, платформенно‑зависимые ключи и правильность PowerShell‑команд для Windows.
│   ├── test_diagnose.py  # Это файл unit‑тестов, проверяющий работу диагностики и её вспомогательных функций в lib/diagnose.py.
│   ├── test_google_calendar.py  # Это файл модульных тестов, проверяющий работу обёртки Google Calendar (авторизацию, создание напоминаний и получение событий) с помощью поддельных сервисов и запросов, чтобы гарантировать корректность функциональности без реального обращения к API.
│   ├── test_google_tasks.py  # Это файл тестов, проверяющий работу обёртки Google Tasks, включая OAuth‑авторизацию и операции с задачами, с использованием поддельных сервисов.
│   ├── test_intent.py  # Это файл тестов, проверяющий корректность работы класса IntentClassifier, отвечающего за распознавание команд с помощью LLM.
│   ├── test_lmstudio.py  # Тестовые файлы для класса LmStudioClient.
│   ├── test_logger.py  # Это файл тестов, проверяющий работу класса Logger и вспомогательных функций логирования.
│   ├── test_main.py  # Это файл тестов, проверяющий функции и классы модуля main, включая исправление кодировки, загрузку модели Vosk, работу TranscriptionWorker и точку входа программы.
│   ├── test_media.py  # Это файл тестов, проверяющий функции is_media_playing и is_media_available из модуля lib.media, обеспечивая корректную работу определения статуса воспроизведения медиа.
│   ├── test_omni.py  # Это файл тестов для класса OmniRouterClient, проверяющий его основные функции, параметры и поведение логирования.
│   ├── test_opencode_cli.py  # Это файл тестов, проверяющий работу и функциональность OpenCodeCliRunner и связанных функций в модуле lib/opencode_cli.py.
│   ├── test_orchestrator.py  # Это файл тестов, проверяющий работу и поведение класса Orchestrator в проекте.
│   ├── test_output.py  # Это тестовый файл, содержащий набор юнит‑тестов для проверки функциональности и логирования класса TranscriptionOutput.
│   ├── test_plans.py  # Это файл тестов, проверяющий работу обработчика планов (PlansHandler) из модуля lib.plans, включая распознавание запросов, выбор текущей задачи из календаря и проверку готовности авторизации.
│   ├── test_provider_manager.py  # Это файл тестов, проверяющий работу класса ProviderManager, его инициализацию, загрузку конфигурации, выбор активного провайдера и создание клиентских экземпляров.
│   ├── test_providers_init.py  # Это файл тестов, проверяющий базовый абстрактный класс BaseLLMClient и его поведение.
│   ├── test_race.py  # Это файл unit‑тестов, проверяющий работу класса RaceClient, его методы, таймауты, обработку ответов и логирование.
│   ├── test_reminders.py  # Файл tests/test_reminders.py содержит набор юнит‑тестов для модуля напоминаний, проверяющих очистку фраз, определение напоминаний, создание спецификаций, взаимодействие с Google Calendar и работу CLI.
│   ├── test_status.py  # Это файл тестов, проверяющий работу класса StatusStore и его функций загрузки, проверок статусов, фонового опроса и встроенных чекеров.
│   ├── test_tasks.py  # Это файл тестов, проверяющий корректность работы обработчика задач TaskHandler и связанных функций в модуле lib.tasks.
│   ├── test_time_parser.py  # Это файл тестов, проверяющий корректность разбора русских фраз о напоминаниях парсером времени.
│   └── test_tts.py  # Это файл тестов, проверяющий работу модуля синтеза речи lib.tts и его командной строки.
├── AGENTS.md  # Руководство для агентов Voice Control: правила коммитов (только по команде), version gate, команды сборки/тестов, архитектура (Vosk STT → Orchestrator), Google Calendar/ Tasks, аудио и стоп-слова, LLM-провайдеры (race), концепция голосовых команд (commands.json, 90% порог, концепция трёх строк).
├── COMMANDS.md  # Это файл‑документация, в котором описаны голосовые команды, их триггеры, соответствующие навыки, параметры и вызываемые скрипты, служит справочником для работы голосового ассистента.
├── README.md  # README.md — это файл описания проекта, содержащий инструкцию по установке, использованию и функционалу голосового помощника для Windows.
├── STRUCTURE.md  # STRUCTURE.md — это документ, описывающий иерархию каталогов и файлов проекта, их назначение и взаимосвязи, чтобы разработчики могли быстро понять структуру кода и компонентов.
├── TODO-skills.md  # Это файл TODO-skills.md, в котором перечислены требуемые функции и команды ассистента, служащий планом задач для их реализации в проекте.
├── TODO.md  # Это файл TODO.md, в котором перечислены текущие задачи и планы разработки проекта.
├── VERSION  # Версия проекта
├── WORKFLOW.md  # WORKFLOW.md — документ, описывающий концепцию, архитектуру и процесс обработки голосовых команд в проекте, включая пайплайн, роли моделей, оркестрацию и TDD‑цикл создания команд и скиллов.
├── WORKING.md  # WORKING.md — файл, фиксирующий текущий статус разработки и планируемые действия, используемый командой для отслеживания прогресса проекта.
├── main.py  # Это основной исполняемый скрипт, который инициализирует и запускает процесс распознавания речи, обработки голосовых команд и синтеза речи в приложении.
├── opencode.json  # Это файл конфигурации opencode.json, в котором указывается схема и список плагинов, необходимых для интеграции проекта с сервисом OpenCode.
├── providers.json  # Файл содержит список доступных провайдеров LLM и активный провайдер по умолчанию.
├── reauth.py  # reauth.py — скрипт, который голосовым оповещением информирует пользователя о необходимости авторизации и запускает процесс авторизации Google Calendar через браузер, используя TTS и библиотеку GoogleCalendar.
└── requirements.txt  # Файл requirements.txt содержит список зависимостей Python и системных компонентов, необходимых для установки и работы проекта.
```
## STRUCTURE:END
## STRUCTURE:END
